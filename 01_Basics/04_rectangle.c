#include<stdio.h>
#include<conio.h>
void main()
{
int a=4, b=5, c=6, area, volume;
printf("Length of the rectangle is %d ,breath of the rectangle is %d and height of the rectangle is %d",a,b,c);

//Area
area=a*b;
printf("Area of the rectangle is %d\n",area);

\\Volume 
volume=a*b*c;
printf("Volume of the rectangle is %d",volume);
getch();
}