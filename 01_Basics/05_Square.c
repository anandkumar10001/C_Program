#include<stdio.h>
#include<conio.h>
void main()
{
int a=5, area, volume;
printf("Side of the square is %d\n",a);

//Area
area=a*a;
printf("Area of the Square is %d\n",area);

//Volume 
volume=a*a*a;
printf("Volume of the Square is %d",volume);

getch();
}